http: ":8080"
ssh: ":2222"
agent_token: 改成隧道口令
access_token: 改成访问token
host_key: ssh_host_key

# 不写这三项就全放行。写了就只放名单里的。
# allow_ips: [127.0.0.1, 10.0.0.0/8]
# allow_clients: [office, home]
# allow_targets: [127.0.0.1:80, 10.0.0.0/8]
